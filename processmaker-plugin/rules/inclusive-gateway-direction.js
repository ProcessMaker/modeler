/**
 * Rule that validates  on bpmn:InclusiveGateway.
 */
module.exports = function() {
  function check(node, reporter) {
    /* @TODO: Implement after https://github.com/ProcessMaker/modeler/pull/266 is merged */
    return;
  }

  return { check };
};

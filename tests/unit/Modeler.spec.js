import Modeler from '../../src/components/Modeler.vue';

describe('registerInspectorExtension', () => {
    it('adds config with a container', () => {
        // Placeholder
        expect(typeof Modeler.created).toBe('function')
    })
})
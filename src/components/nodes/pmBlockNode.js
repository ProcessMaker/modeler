import Node from './node';

export default class PmBlockNode extends Node {
//   static timerPropertyKeys = ['timeDuration', 'timeDate', 'timeCycle'];
    // cloneFlow(nodeRegistry, moddle, $t) {
    //     return 
    //     // return this.pmBlock(super.clone(nodeRegistry, moddle, $t));
    // }
}

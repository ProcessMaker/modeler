/**
 * This function was added purely to test our jest setup is working.
 * It should be removed along with the corresponding test.
 * @returns {number}
 */
export function bar() {
  return Math.random();
}
